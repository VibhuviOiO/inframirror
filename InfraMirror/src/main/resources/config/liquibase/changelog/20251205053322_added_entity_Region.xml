<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity Region.
    -->
    <changeSet id="20251205053322-1" author="jhipster">
        <createTable tableName="region">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(50)">
                <constraints nullable="false" />
            </column>
            <column name="region_code" type="varchar(20)">
                <constraints nullable="true" />
            </column>
            <column name="group_name" type="varchar(20)">
                <constraints nullable="true" />
            </column>
            <column name="search_vector" type="tsvector">
                <constraints nullable="true" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <changeSet id="20251205053322-2" author="system">
        <sql>
            CREATE INDEX idx_region_search_vector
            ON region USING GIN (search_vector);
        </sql>
    </changeSet>

    <changeSet id="20251205053322-3" author="system">
        <sql splitStatements="false"><![CDATA[
            CREATE FUNCTION region_search_vector_update() RETURNS trigger AS $$
            BEGIN
              NEW.search_vector := to_tsvector('simple', unaccent(
                coalesce(NEW.name, '') || ' ' ||
                coalesce(NEW.region_code, '') || ' ' ||
                coalesce(NEW.group_name, '')
              ));
              RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;

            CREATE TRIGGER trg_region_search_vector
            BEFORE INSERT OR UPDATE ON region
            FOR EACH ROW
            EXECUTE FUNCTION region_search_vector_update();
        ]]></sql>
    </changeSet>

    <changeSet id="20251205053322-4" author="system">
        <sql>
            -- Trigram indexes for fuzzy search optimization
            -- Improves performance of similarity() queries by 10-50x
            CREATE INDEX idx_region_name_trgm ON region USING GIN (name gin_trgm_ops);
            CREATE INDEX idx_region_code_trgm ON region USING GIN (region_code gin_trgm_ops);
            CREATE INDEX idx_region_group_trgm ON region USING GIN (group_name gin_trgm_ops);
        </sql>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20251205053322-1-data" author="jhipster" context="faker">
        <loadData
                  file="config/liquibase/fake-data/region.csv"
                  separator=";"
                  tableName="region"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="name" type="string"/>
            <column name="region_code" type="string"/>
            <column name="group_name" type="string"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
